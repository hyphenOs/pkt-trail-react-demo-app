const config = {
  appConfig: {
    websocketServer: "ws://localhost:3030",
  },
  dashboardConfig: {
    showSelectedDetails: true,
  },
  tableConfig: {
    packetWindowSize: 50,
    jumpSize: 1,
    autoscroll: true,
  },
  detailsConfig: {
    expanded: false,
  },
};

export default config;
